export default function VerifyRequestPage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-sm flex-col justify-center gap-4 px-4 text-center">
      <h1 className="text-2xl font-semibold">Revisa tu correo</h1>
      <p className="text-sm text-gray-600">
        Si el correo que escribiste está registrado en el programa, te enviamos un enlace de
        acceso. Puede tardar unos minutos y, a veces, cae en la carpeta de spam o promociones.
      </p>
    </main>
  );
}
